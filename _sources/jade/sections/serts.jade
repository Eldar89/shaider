|<?php 
|  $args = array(
|    'post_type' => 'sert', //Тип поста
|    'posts_per_page' => -1,//Постов на одной странице
|    'category_name' => 'sert' //Категория постов
|  );
|?>

section#serts
  header Сертификаты и лицензии
  .container-own
    //- .arrows
    //-   .line
    //-   .arrows_block
    //-     .arrow_left
    //-     .arrow_right
    //-   .line
    .slider_serts
      |<?php
      |  $lastBlog = new WP_Query($args);
      |  if( $lastBlog->have_posts() ):
      |    while($lastBlog->have_posts() ): $lastBlog->the_post();
      |?>
      |     <?php $sert = types_render_field( "sert_images", array('raw' => true) ); ?>
      a(href!="<?php echo $sert; ?>" class="fancybox_sert" rel="gallary_sert").slider_elem
        img(src!="<?php echo $sert; ?>").letter
        img(src!="<?php echo get_template_directory_uri();?>/images/border_sert.png").border
      |<?php
      |    endwhile;
      |  endif;
      |?>
